# Managed by vpsmgr — generated file, do not edit by hand.
# Changes are overwritten on the next write; use the panel / `vps` CLI instead.
#
# BOOTSTRAP COPY ONLY: the first generation has no domains yet. The panel's
# publisher (internal/hpx) writes the real file, containing one HTTP backend,
# one TCP (443) backend and the routing rules per domain.
